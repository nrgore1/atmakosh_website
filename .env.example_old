# ================================
# Required for production (Hostinger)
# Set these in hPanel → Node.js → Environment Variables
# ================================

SITE_URL=https://atmakosh.com
ADMIN_PASSWORD=change_this_in_hostinger

# Database
DB_HOST=
DB_USER=
DB_PASS=
DB_NAME=

# SMTP (email notifications)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=false
SMTP_FROM=

# PDF token expiry (minutes)
PDF_TOKEN_MINUTES=60
