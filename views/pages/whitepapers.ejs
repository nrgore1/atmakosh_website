<%- include('../partials/head', { SITE }) %>
<%- include('../partials/nav', { SITE, path }) %>

<section class="section">
  <h1>Whitepapers</h1>
  <p class="lead">
    Full PDFs are available during the invite-only preview. If you’re approved, generate secure download links below.
  </p>

  <div style="margin:18px 0 28px;">
    <a class="btn btn--primary" href="/whitepapers/access">Access PDFs (Approved Users)</a>
    <a class="btn btn--ghost" href="/invite" style="margin-left:10px;">Request Invitation</a>
  </div>

  <div class="grid grid--2">
    <% papers.forEach(p => { %>
      <div class="card">
        <h3><%= p.title %></h3>
        <p class="muted">Secure downloads are provided via expiring links after approval.</p>
      </div>
    <% }) %>
  </div>
</section>

<%- include('../partials/footer') %>
