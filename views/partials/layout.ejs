<!doctype html>
<html lang="en">
  <head>
    <% include ./head.ejs %>
  </head>
  <body>
    <div class="bg">
      <div class="bg__mandala" aria-hidden="true">
        <svg viewBox="0 0 800 800">
          <defs>
            <radialGradient id="g1" cx="50%" cy="50%" r="60%">
              <stop offset="0%" stop-color="#E65AA5" stop-opacity="0.35"/>
              <stop offset="55%" stop-color="#FF7A18" stop-opacity="0.18"/>
              <stop offset="100%" stop-color="#050B2E" stop-opacity="0"/>
            </radialGradient>
          </defs>
          <circle cx="400" cy="400" r="330" fill="url(#g1)"/>
        </svg>
      </div>
      <div class="bg__grain"></div>
    </div>

    <% include ./nav.ejs %>

    <main class="main">
      <%- body %>
    </main>

    <% include ./footer.ejs %>
  </body>
</html>
